# Mimir evaluation harness (python -m mimir_eval). Python 3.10+.
httpx>=0.27
pyyaml>=6.0
numpy>=1.24
pandas>=2.0
scipy>=1.11
matplotlib>=3.7
openpyxl>=3.1
tabulate>=0.9          # pandas.to_markdown
sentence-transformers>=2.7   # e5 embeddings for duplicate rate, coverage, gold similarity
pytest>=7              # unit tests
