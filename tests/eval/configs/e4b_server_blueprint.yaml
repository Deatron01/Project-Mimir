# E4b - the blueprint pipeline (E2 settings) on the university GenAI server (RQ4 with the new pipeline).
# Replace the pipeline block with the best of E1-E3 once those results are in.
# Data leaves the machine: only use documents whose license allows it.
name: E4b
description: "Blueprint + verifier on qwen38udq8xl (university GenAI server)"
seeds: [1, 2, 3]
exam: {n_questions: 10, types: [mcq]}
pipeline: {kind: blueprint, retrieval: dense, retrieval_k: 3, verifier: true, blind_test: true, max_retries: 2, graph: false}
chunking: {mode: service, method: percentile, threshold: 85, target_size: 1000}
generator: {provider: genai, model: "qwen38udq8xl", temperature: 0.2}
